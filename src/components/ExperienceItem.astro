---
import ArrowIcon from './icons/ArrowIcon.astro'

interface Props {
    date: string
    company: string
    role: string
    technologies: string[],
    link?: string
}

const { date, company, role, technologies, link } = Astro.props
---

<li class="min-h-40 mb-10 group/item relative cursor-pointer transition-all">
    <span class="absolute dot h-3 w-3 rounded-full bg-teal-400"></span>
    <a href={link} target="_blank">
        <div class="group-hover/item:bg-slate-800/50 group-hover/list:opacity-50 group-hover/item:!opacity-100 p-4 rounded-md transition-all">
            <div class="flex justify-between">
                <time class="text-xs font-semibold uppercase tracking-wide">{date}</time>
                <div class="h-5 w-5 group-hover/item:-translate-y-1 duration-200">
                    <ArrowIcon class="absolute h-5 w-5 group-hover/item:text-teal-400" />
                </div>
            </div>
            <h3 class="font-medium text-slate-200 leading-tight my-2 group-hover/item:text-teal-400">{company}</h3>
            <h3  class="text-sm font-semibold uppercase tracking-wide">{role}</h3>

            <div class="text-sm lead tracking-wider mt-8">
                <slot />
            </div>

            <ol class="flex flex-wrap gap-2 mt-8">
                {
                    technologies.map( technology => 
                        <li class="rounded-full bg-teal-400/10 px-3 py-1 text-xs font-medium leading-5 text-teal-300">
                            {technology}
                        </li>
                    )
                }
            </ol>
        </div>
    </a>
</li>

<style>
    .dot {
        top: 18px;
        left: -7px;
    }
</style>